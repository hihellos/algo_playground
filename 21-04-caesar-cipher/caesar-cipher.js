// Write a function that takes in a string and and an offset and returns a new string with each character moved by the offset

var caesarCipher = function(str, offset) {
    // var to hold new string with shifted letters
        // letters must retain case (if character == character.toUpperCase() )
    let ceasarString = "";

    // for loop to step thorugh each letter in string
    
    // return new string
    console.log(ceasarString);
    return ceasarString;
};
